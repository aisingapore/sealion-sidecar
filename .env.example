# SEA-LION Sidecar — environment variables
#
# Required when allow_env_key_fallback: true in config.yaml.
# Ignored otherwise — clients supply their own key via Authorization: Bearer.
# SEALION_API_KEY=your_api_key_here
